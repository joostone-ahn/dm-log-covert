version: '3.8'

services:
  dm-log-converter:
    build: .
    container_name: dm-log-converter
    ports:
      - "9090:9090"
    volumes:
      - ./logs:/logs
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
